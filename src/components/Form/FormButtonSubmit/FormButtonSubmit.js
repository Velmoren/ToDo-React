import { FormButton } from './FormButtonSubmit.styled'
import plus from '../../../assets/icons/plus.png'

const FormButtonSubmit = () => {
    return (
        <FormButton type={'submit'} icon={plus} />
    );
};

export default FormButtonSubmit;